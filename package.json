{
  "name": "poker-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Texas Hold'em analyzer with Monte Carlo simulation",
  "scripts": {
    "dev": "npx wrangler pages dev .",
    "build:wasm": "sh wasm/build.sh",
    "test": "node --test tests/**/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check": "npm run typecheck && npm run test",
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "sharp": "^0.34.5",
    "typescript": "^5.9.2"
  }
}
